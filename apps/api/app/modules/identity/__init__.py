"""
Identity Context — contains the Authentication bounded context.

All identity and access management concerns live under this namespace.
Sub-modules must not import from each other's internal layers.
"""
