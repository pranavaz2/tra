"""
Authentication Domain Layer.

Contains: entities, value objects, domain events, repository interfaces,
service policy interfaces, and specifications.

Zero external dependencies — no FastAPI, no SQLAlchemy, no Redis, no JWT.
"""
