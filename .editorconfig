; =============================================================================
; Travix AI — .editorconfig
; Enforces consistent formatting across all editors and IDEs.
; Covers: Python, Dart, YAML, JSON, Markdown, Shell, Makefile, SQL
; =============================================================================

root = true

; ---------------------------------------------------------------------------
; All files: sensible defaults
; ---------------------------------------------------------------------------
[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 4
insert_final_newline = true
trim_trailing_whitespace = true
max_line_length = 100

; ---------------------------------------------------------------------------
; Python — Black-compatible (100 chars)
; ---------------------------------------------------------------------------
[*.py]
indent_style = space
indent_size = 4
max_line_length = 100

[*.pyi]
indent_style = space
indent_size = 4

; ---------------------------------------------------------------------------
; Dart — dart format standard (2 spaces)
; ---------------------------------------------------------------------------
[*.dart]
indent_style = space
indent_size = 2
max_line_length = 100

; ---------------------------------------------------------------------------
; YAML — 2 spaces (Docker Compose, GitHub Actions, pre-commit, pubspec)
; ---------------------------------------------------------------------------
[*.{yaml,yml}]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; JSON — 2 spaces
; ---------------------------------------------------------------------------
[*.json]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; TOML — 4 spaces (pyproject.toml, Cargo.toml)
; ---------------------------------------------------------------------------
[*.toml]
indent_style = space
indent_size = 4

; ---------------------------------------------------------------------------
; Makefile — MUST use tabs (make requirement)
; ---------------------------------------------------------------------------
[Makefile]
indent_style = tab
indent_size = 4

; ---------------------------------------------------------------------------
; SQL — 4 spaces
; ---------------------------------------------------------------------------
[*.sql]
indent_style = space
indent_size = 4

; ---------------------------------------------------------------------------
; Shell scripts
; ---------------------------------------------------------------------------
[*.{sh,bash}]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; Markdown — preserve trailing spaces (used for hard line breaks in some docs)
; Do NOT trim trailing whitespace in .md files
; ---------------------------------------------------------------------------
[*.md]
trim_trailing_whitespace = false
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; Dart pubspec
; ---------------------------------------------------------------------------
[pubspec.yaml]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; Docker
; ---------------------------------------------------------------------------
[Dockerfile*]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; GitHub Actions
; ---------------------------------------------------------------------------
[.github/**/*.yml]
indent_style = space
indent_size = 2

; ---------------------------------------------------------------------------
; Alembic migration files
; ---------------------------------------------------------------------------
[apps/api/migrations/versions/*.py]
indent_style = space
indent_size = 4
